# Project Startup Plan&#10;&#10;## 1. Install dependencies&#10;- [x] Run `npm install` to ensure all packages (express, bcryptjs, etc.) are installed&#10;  (Executed successfully)&#10;&#10;## 2. Start the server&#10;- [ ] Run `npm start` (executes `node server.js`)&#10;  - Server auto-creates users.json with defaults: admin/admin123, customer/customer123&#10;  - Serves app at http://localhost:3000&#10;&#10;## 3. Verify running&#10;- [ ] Check terminal: "EventFlow backend running on http://localhost:3000"&#10;- [ ] Open http://localhost:3000 in browser&#10;- [ ] Test login with default accounts&#10;&#10;## 4. Test functionality&#10;- [ ] Create events (admin), RSVP, view reports
